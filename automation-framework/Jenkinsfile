pipeline {
    agent any

    stages {
        // Option A: Let Jenkins handle checkout automatically (Stage can be omitted or kept simple)
        stage('Checkout') {
            steps {
                echo 'Repository checked out automatically by Jenkins.'
            }
        }

        stage('Build & Test') {
            steps {
                dir('automation-framework') {
                    sh 'mvn clean test'
                }
            }
        }

        stage('Containerized Execution') {
            steps {
                dir('automation-framework') {
                    sh 'docker build -t automation-framework .'
                    sh 'docker run --rm automation-framework'
                }
            }
        }
    }

    post {
        always {
            dir('automation-framework') {
                junit allowEmptyResults: true, testResults: '**/target/surefire-reports/*.xml'
            }
        }
    }
}